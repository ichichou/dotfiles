pacman::p_load(
  pacman,
  conflicted,
  tidyverse,
  lubridate,
  stats,
  MASS
)

conflicted::conflicts_prefer(
  dplyr::filter,
  dplyr::lag,
  dplyr::select,
  .quiet = TRUE
)

source("~/dotfiles/rlang/function.R")

clear_globalenv()
